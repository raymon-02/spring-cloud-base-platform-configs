messages:
  hello: Hello from info and github!

calculate-server:
  ribbon:
    ReadTimeout: 5000

feign:
  hystrix:
    enabled: true

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 5000
