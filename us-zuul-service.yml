zuul:
  prefix: /
  routes:
    api-service:
      path: /api-service/**
      serviceId: api-server

api-service:
  ribbon.ReadTimeout: 5000

hystrix:
  command:
    api-service:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 5000
